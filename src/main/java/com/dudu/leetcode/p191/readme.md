一开始我是从不断除以2来入手，但发现对于负数需要单独处理。
查询原码，反码和补码的知识，但发现对这个问题帮助并不大。
因为在java中，int是以补码的形式来存储的。补码加一的存在使得把负数转成正数再处理还需要单独考虑与实际1的数目的对应关系。
后来发现`>>`这个符号，如果通过不断取出最后一位的bit来判断是否为0来叠加1的数目，就很好的满足了题目要求。
不过在这个过程中，发现了`>>>`和`>>`的区别。`>>`是进位符号位的值。